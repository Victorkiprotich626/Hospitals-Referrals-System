spring:
  datasource:
    url: jdbc:h2:mem:referrals;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

app:
  storage:
    attachments-dir: target/test-storage/referral-attachments
